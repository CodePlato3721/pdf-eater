- Dev: PD-10's `/api/ask` e2e test (`backend/tests/e2e/test_ask_hyde_e2e.py`) hits the real OpenAI API and is marked `@pytest.mark.smoke` (costs money, excluded by default). Considered switching it to a fully local flow instead — a Docker-run Ollama (tiny model) providing local embeddings + LLM, with a committed FAISS fixture built from `metamorphosis.pdf` — but that's infra-level scope beyond this task: no local-inference dependency exists yet (`langchain-ollama` isn't installed), `backend/core/embeddings.py`/`core/chain.py` hardcode `OpenAIEmbeddings`/`ChatOpenAI` with no provider-switching abstraction, and there's no committed vectorstore fixture today (`backend/data/faiss_index/` is gitignored runtime state from whatever PDF was last uploaded locally, not reproducible test data). Revisit as a follow-up ticket: needs a design decision on the provider-abstraction approach before implementation.
