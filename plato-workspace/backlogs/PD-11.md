- Dev: During PD-11 TASK-01 review, considered moving `backend/core/loader.py` (`load_and_split`, `is_readable`) into `backend/utils/`. Out of scope for this task and doesn't fit the `utils/` layer's definition just established in `plato-workspace/role-rules/coder/COD.md` (framework-agnostic, dependency-free, no business/domain logic) — `loader.py` depends on `langchain_community`, `langchain_text_splitters`, `pdfminer`, and has PDF-domain judgment calls (e.g. the "readable text" threshold), so it belongs in `core/` per the existing convention. If revisited, the actual question is whether it needs a more specific home (e.g. `core/pdf/`), not `utils/`.
- Dev: During PD-11 TASK-01 review, considered renaming `backend/core/http_logging.py` to `logging_http_client.py` (matching its exported `create_logging_http_client` function name). Out of scope for this task — unrelated to uploaded-file persistence — and touches 3 files outside this task's Design (`core/chain.py`, `core/embeddings.py`, `tests/e2e/test_ask_hyde_e2e.py`). Straightforward rename if picked up as its own task.
