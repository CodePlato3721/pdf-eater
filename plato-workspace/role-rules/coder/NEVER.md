# NEVER

- Follow Chicago-style testing (assert observable state/outcomes), not London-style (mockist) — do not write unit tests that only assert a function's internal calls/mocks fired with certain arguments when the function itself has no branching or transformation logic worth protecting.
